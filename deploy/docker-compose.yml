version: "3.9"

services:
  tasks:
    build:
      context: ..
      dockerfile: services/tasks/Dockerfile
    container_name: tasks_service
    ports:
      - "8082:8082"
    environment:
      TASKS_PORT: "8082"
